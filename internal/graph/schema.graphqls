# GraphQL schema example
#
# https://gqlgen.com/getting-started/

scalar DateTime

type User {
  id: ID!
  name: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  apiKey: String!
}

type Query {
  User: User!
}

type Mutation {
  CreateUser(name: String!): User
}
